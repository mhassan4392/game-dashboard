import BrowserNavbar from "./BrowserNavbar";
import MobileNavbar from "./MobileNavbar";

const Navbar = () => {
  return (
    <div>
      <BrowserNavbar />
      <MobileNavbar />
    </div>
  );
};

export default Navbar;
